package com.trump.auction.trade.pay.alipay;

/*import com.alibaba.fastjson.JSONObject;
import com.alipay.api.AlipayApiException;
import com.alipay.api.AlipayClient;
import com.alipay.api.DefaultAlipayClient;
import com.alipay.api.domain.AlipayTradeAppPayModel;
import com.alipay.api.request.AlipayTradeAppPayRequest;
import com.alipay.api.response.AlipayTradeAppPayResponse;*/


public class Test {
	
	public static void main(String[] args) {
		
	/*	String appid = "";//jedisCluster.get("ALIPAY_APPID")
		String privateKey = "";//jedisCluster.get("ALIPAY_PRIVATE_KEY")
		String outTradeNo = "";
		String totalAmount = "";
		String notifyUrl = "";//jedisCluster.get("ALIPYA_CALLBACK_URL");
		AlipayClient alipayClient = new DefaultAlipayClient(AlipayConfig.gateway_url, 
				appid,privateKey,AlipayConfig.format, AlipayConfig.input_charset, AlipayConfig.ali_public_key, AlipayConfig.sign_type); 
        AlipayTradeAppPayRequest payRequest = new AlipayTradeAppPayRequest();
        AlipayTradeAppPayModel model = new AlipayTradeAppPayModel();
        model.setBody("企鹅抓娃娃游戏币充值");
        model.setSubject("充值");
        model.setOutTradeNo(outTradeNo);
        model.setTimeoutExpress("30m");
        model.setTotalAmount(totalAmount);
        //model.setTotalAmount("0.01");
        model.setProductCode("QUICK_MSECURITY_PAY");
        payRequest.setBizModel(model);
        payRequest.setNotifyUrl(notifyUrl);
        AlipayTradeAppPayResponse payResponse = new AlipayTradeAppPayResponse();
        try {
                //这里和普通的接口调用不同，使用的是sdkExecute
                payResponse = alipayClient.sdkExecute(payRequest);
            } catch (AlipayApiException e) {
                e.printStackTrace();
        }
        String info =  payResponse.getBody();*/
	}
}
